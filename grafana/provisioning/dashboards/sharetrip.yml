apiVersion: 1

providers:
  - name: ShareTrip
    orgId: 1
    folder: ShareTrip
    folderUid: sharetrip
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
